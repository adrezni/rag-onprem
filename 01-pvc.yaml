apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: granite-vision-model-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: gp3-csi
